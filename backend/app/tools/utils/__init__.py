"""
tools.utils
===========

Shared helper functionality used by multiple tools/services.

Contains:
- tool_helpers.py : Generic, reusable helper functions (e.g. input
  normalization, logging helpers, timing decorators, structured error
  formatting).

This module does NOT contain tool-specific or service-specific logic —
only generic helpers safe to reuse anywhere in the tools layer.
"""
